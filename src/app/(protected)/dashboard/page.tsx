import { LogoutButton } from "@/components/buttons/LogoutButton";

export default function DashboardPage() {
      return (
            <h1>
                  <LogoutButton>
                        Sair
                  </LogoutButton>
            </h1>
      )
}